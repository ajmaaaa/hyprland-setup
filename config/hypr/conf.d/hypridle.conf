# █░█ █▄█ █▀█ █▀█ █ █▀▄ █░░ █▀▀
# █▀█ ░█░ █▀▀ █▀▄ █ █▄▀ █▄▄ ██▄
# ============================================================
# Replacement for swayidle — using hypridle

general {
    lock_cmd         = pidof hyprlock || hyprlock  # Lock only
    before_sleep_cmd = loginctl lock-session       # Lock before suspend
    ignore_dbus_inhibit = false
}

# Warning before screen turns off (optional notification)
listener {
    timeout  = 270
    on-timeout = notify-send -u low -t 30000 "Idle" "Screen will turn off in 30s..."
}

# Lock screen after 300 seconds of idle
listener {
    timeout  = 300
    on-timeout = loginctl lock-session
}
